{
  "name": "@browser-mcp/security",
  "version": "1.0.0",
  "description": "Cryptographic token verification, origin protection, and PII redaction for BrowserMCP",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@browser-mcp/shared": "*",
    "@browser-mcp/protocol": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
